modules += types
modules += mem
modules += char

modules += math

# modules += num
# modules += numarr

# modules += strarr

# TODO:
# modules += str
# modules += utf8
# modules += color
# modules += printf
# modules += thread-pool
# modules += linked-list
# modules += hashmap
# modules += terminal
# modules += serialize
# modules += binary-tree
# modules += arg-parser
# modules += neural-network
